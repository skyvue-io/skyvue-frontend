import ReactLoading from 'react-loading-components';
import React from 'react';
import Styles from 'styles/Styles';

const Loading: React.FC = () => <ReactLoading type="bars" fill={Styles.purple400} />;

export default Loading;
